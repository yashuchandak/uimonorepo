{
  "name": "ui-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "ts-loader": "^9.5.4"
  }
}
